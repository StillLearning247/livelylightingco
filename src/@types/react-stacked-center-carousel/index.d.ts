declare module "react-stacked-center-carousel" {
  // You can be more precise here if you want, but this is the minimal declaration:
  const StackedCarousel: any;
  const ResponsiveContainer: any;
  export { StackedCarousel, ResponsiveContainer };
}
